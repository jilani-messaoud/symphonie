<form method="post" action="{{ path('app_projec_delete', {'id': projec.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projec.id) }}">
    <button class="btn">Delete</button>
</form>
